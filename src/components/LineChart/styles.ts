import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  mainContainer: {
    width: '100%',
    justifyContent: 'center',
  },
  chartContainer: {
    width: '100%',
    height: '98%',
    justifyContent: 'center',
  },
  XbuttonContainer: {
    zIndex: 100,
    position: 'absolute',
    top: 0,
    right: 20,
  },
  XbuttonTitle: {
    fontSize: 20,
    color: 'red',
  },
});
