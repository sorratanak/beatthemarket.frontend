import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  burgerContainer: {
    position: 'absolute',
    top: 0,
    left: 0,
    zIndex: 1,
    width: 50,
    height: 50,
    backgroundColor: 'red',
  },
});

export default styles;
