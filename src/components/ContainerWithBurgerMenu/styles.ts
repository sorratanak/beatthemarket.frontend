import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  burgerContainer: {
    position: 'absolute',
    top: 12,
    left: 12,
    zIndex: 1,
  },
  burgerImage: {
    width: 50,
    height: 50,
  },
});

export default styles;
