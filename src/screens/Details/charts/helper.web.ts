import {
  VictoryChart,
  VictoryScatter,
  VictoryLine,
  VictoryZoomContainer,
} from 'victory';

const style: { [key: string]: React.CSSProperties } = {
  parent: {
    flex: 1,
    maxWidth: '80%',
    justifyContent: 'center',
  },
};

export {
  VictoryScatter,
  VictoryLine,
  VictoryChart,
  VictoryZoomContainer,
  style,
};
