import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  textInputStyle: {
    borderWidth: 1,
    borderColor: 'grey',
    width: '80%',
    height: 50,
    marginTop: 10,
    textAlign: 'center',
  },
});
